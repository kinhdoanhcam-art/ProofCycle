# ProofCycle v2.0 / runtime procedure

Contract `0x2B37e48581D888cc635Fd716456328F1411700D7`, source SHA256 `6070ef9c487e5fafbb8141e1d6c2722fea94333a218f40a79f9665049b9c7b0b`. Deployment was FINALIZED/SUCCESS and `get_config` matched. The observed live proof and exact transaction hashes are in [RUNTIME_EVIDENCE.md](RUNTIME_EVIDENCE.md). These steps are for independent reproduction, not instructions to replay already settled periods. Follow one write transaction at a time; preserve tx hash plus finalized `get_obligation`, `get_period` and `get_report` results. If a transaction fails, inspect the full execution result and unchanged state. Never infer a semantic PASS from a status field alone.

1. On the Vercel site confirm `PROFILE MATCH` and pinned address; remove an old `VITE_CONTRACT_ADDRESS` environment override if `WRITES LOCKED`. Verify Vercel source commit contains the source SHA above. `CẦN SNAP: CÓ` — page badge/address and `get_config`.
2. Connect creator wallet and create with **distinct** responsible and issuer addresses, source name that accurately identifies an issuer-controlled evidence source, fixed requirement, period/submission/remediation windows (for example 1200/1100/1100 seconds). Do not assert real-world identity merely from the address. Verify new count +1 and get_obligation roles/immutable fields. `CẦN SNAP: CÓ` — transaction and finalized obligation.
3. After a period ends and before its submission deadline, connect responsible wallet. Use “Test refusal without issuer report” exactly once. Verify the finalized transaction's `consensus_data.leader_receipt` has `result.status = rollback`, `execution_result = ERROR`, and the expected payload, plus unchanged finalized obligation, period, report. A successful EVM receipt or `MAJORITY_AGREE` alone does not prove GenVM execution success. Observed on obligation #1 period #2, tx `0x381c9bdbde0cd7a59a49ea649f004e3d147fb1b430c58fce3ccf91c927eee36e`, payload `Authenticated period report is required`. `CẦN SNAP: CÓ` — execution and state.
4. Connect issuer wallet. Attest an **actual record reference** that independently supports period 1, such as a full Explorer transaction hash, plus its observed result and period-specific explanation. Check signed tx, stored `get_report(1,1,false)` `exists`, issuer, bounds, reference, exact observations, digest and `get_period` digest. Attestation alone cannot update verdict or counters. `CẦN SNAP: CÓ`.
5. Connect responsible wallet. Evaluate exact stored digest. Check finalized initial verdict (SATISFIED or DEFICIENT), report unchanged, semantic eval count delta, and no settlement counters. A real semantic GAP/FRESH cannot be predetermined. `CẦN SNAP: CÓ`.
6. If initial verdict is DEFICIENT and remediation remains open, issuer signs a distinct remediation report for that same period, responsible evaluates its stored remediation digest. Confirm initial deficiency retained, final outcome and cache flag. Exact repeat observations in same obligation and period can yield HIT; a new period must not reuse that cache. `CẦN SNAP: CÓ` for each state-changing transaction.
7. Connect any wallet and settle once the next period is ready. Inspect ordered cursor, every settled row, exact counters, streak and derived status. For a missed period wait until **after** its submission deadline. `CẦN SNAP: CÓ` — transaction and finalized ledger.

The home/Proof page links to observed runtime transactions. This does not mean a signed issuer statement independently verifies external facts. Independently assess the issuer and external records before claiming factual compliance. Never replay a create just to re-check a missing screenshot; read state first.
